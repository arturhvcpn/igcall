import { Meta } from '@storybook/addon-docs'
import { fontWeights } from '@ignite-ui/tokens'
import { TokensTable } from '../../components'

<Meta title="Tokens/Fonts Weights" />

# Fonts Weights

Essas são os pesos das fontes utilizadas no Ignite UI.

<TokensTable tokens={fontWeights} />